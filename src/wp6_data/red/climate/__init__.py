"""Red's climate model: weather → greenhouse level → per-height wire.

Three links, each trained where the data supports it. See
``MODEL_PAPER.md`` in this package for how it works and what was rejected, and
``docs/red/wire-data-coverage.md`` for the measurements behind the config.
"""
